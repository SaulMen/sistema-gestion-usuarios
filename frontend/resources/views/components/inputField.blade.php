<div class="input-group">
    <label for="{{$name}}" class="label-text">{{ $label}}</label>
    <input type="{{$type}}" id="{{$name}}" name="{{$name}}" class="input-field" value="{{$value}}" {{$readonly}}>
</div>